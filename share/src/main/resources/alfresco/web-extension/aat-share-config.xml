<alfresco-config>

	<!-- Document Library -->
	<config evaluator="string-compare" condition="DocumentLibrary">
		<aspects>
			<visible>
				<aspect name="aat:Transcriber" />
			</visible>

			<addable>
			</addable>

			<removeable>
			</removeable>
		</aspects>
	</config>

</alfresco-config>